# 🚀 Деплой на Vercel

## Подготовка

1. **Убедитесь что проект готов:**
   ```bash
   npm run build
   ```

2. **Проверьте переменные окружения в `.env`**

## Способ 1: Автоматический деплой через GitHub

1. **Загрузите код в GitHub репозиторий**

2. **Зайдите на [vercel.com](https://vercel.com) и подключите репозиторий**

3. **Настройте переменные окружения в Vercel Dashboard:**
   - `REACT_APP_SUPABASE_URL` = `https://znpsiawxdtcjyalfkmqn.supabase.co`
   - `REACT_APP_SUPABASE_ANON_KEY` = ваш anon key из Supabase
   - `REACT_APP_TELEGRAM_BOT_USERNAME` = имя вашего бота без @

4. **Vercel автоматически соберет и задеплоит проект**

## Способ 2: Ручной деплой через CLI

1. **Установите Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Задеплойте проект:**
   ```bash
   vercel --prod
   ```

4. **Настройте переменные окружения:**
   ```bash
   vercel env add REACT_APP_SUPABASE_URL
   vercel env add REACT_APP_SUPABASE_ANON_KEY  
   vercel env add REACT_APP_TELEGRAM_BOT_USERNAME
   ```

## После деплоя

1. **Получите URL от Vercel** (например: `https://your-app.vercel.app`)

2. **Обновите URL в боте:**
   ```python
   # В cfsick/main.py
   CONFIG = {
       "WEBAPP_URL": "https://your-app.vercel.app",  # Новый URL
       # ... остальные настройки
   }
   ```

3. **Перезапустите бота**

## Проверка

1. Откройте бота в Telegram
2. Нажмите "🎰 ИГРАТЬ"
3. Убедитесь что сайт открывается и работает

## Troubleshooting

### Ошибка "Failed to load resource"
- Проверьте переменные окружения в Vercel Dashboard
- Убедитесь что CORS настроен в Supabase

### Telegram WebApp не открывается
- Проверьте что URL в боте обновлен
- Убедитесь что сайт доступен по HTTPS

### Игры не работают
- Проверьте подключение к Supabase
- Убедитесь что все таблицы созданы в базе данных

## Полезные команды

```bash
# Локальная сборка для проверки
npm run build

# Предпросмотр продакшен сборки
npm run preview

# Проверка статуса деплоя
vercel ls

# Просмотр логов
vercel logs
```