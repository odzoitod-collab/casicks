# 🎰 Интеграция Telegram Бота и Казино

Полная интеграция между Telegram ботом (Python/Aiogram) и веб-казино (React/TypeScript) через Supabase с серверной логикой игр и системой "подкрутки".

## 🚀 Ключевые особенности

### ✅ Единая экосистема
- **Telegram WebApp** интегрирован с ботом
- **Realtime синхронизация** данных между ботом и сайтом
- **Серверная логика игр** на основе win_rate из базы данных
- **Автоматическая авторизация** через Telegram

### 🎮 Серверная логика "подкрутки"
- Результат игры определяется **НА СЕРВЕРЕ** на основе `win_rate` пользователя
- Если win_rate = 90%, то игрок выиграет в 90% случаев
- Анимация подстраивается под **заранее вычисленный** результат
- Невозможно "взломать" через клиент

### 🔄 Realtime обновления
- Воркер меняет баланс в боте → мгновенно обновляется на сайте
- Воркер меняет win_rate → следующая игра использует новый процент
- Используется Supabase Realtime channels

### 👤 Профиль пользователя
- **Фото профиля** автоматически загружается из Telegram
- **Никнейм** синхронизируется при каждом входе
- **Баланс** отображается в реальном времени

## 📁 Структура проекта

```
├── cfsick/                          # Telegram бот
│   ├── main.py                      # Основной код бота с панелью воркера
│   └── baza.sql                     # SQL схема базы данных
│
└── pin-up-mobile-casino/            # Веб-казино
    ├── api/
    │   └── supabase.ts              # API с серверной логикой игр
    ├── components/
    │   ├── GameModal.tsx            # Игры с серверной логикой
    │   ├── Header.tsx               # Шапка с фото профиля
    │   └── ...
    ├── App.tsx                      # Главный компонент с Realtime
    ├── SETUP.md                     # Подробная инструкция по настройке
    └── ...
```

## 🎯 Реализованные игры

### 🎰 Слоты
- Серверная логика определяет выигрышную/проигрышную комбинацию
- Анимация спиннинга подстраивается под результат

### 🎡 Колесо фортуны
- Игрок выбирает цвет (Красное/Черное/Зеленое)
- Сервер принудительно останавливает колесо на нужном секторе

### 🥤 Наперстки
- Сервер определяет, под каким стаканом будет мяч
- Анимация перемешивания не влияет на результат

### 🎲 Кости
- Простая игра с визуальной анимацией
- Результат определяется сервером

## ⚙️ Панель воркера

Команда `/worker` в боте открывает панель управления:

- 📊 **Список рефералов** с балансом и win_rate
- 💰 **Изменение баланса** любого пользователя
- 🍀 **Изменение процента удачи** (0-100%)
- 🚫 **Бан/разбан** пользователей
- 🔗 **Реферальная ссылка** для привлечения новых игроков

## 🚀 Деплой на Vercel

### Подготовка к деплою
1. Убедитесь, что все переменные окружения настроены в `.env`
2. Проект готов к деплою с конфигурацией `vercel.json`

### Автоматический деплой
1. Подключите репозиторий к Vercel
2. Настройте переменные окружения в панели Vercel:
   - `REACT_APP_SUPABASE_URL`
   - `REACT_APP_SUPABASE_ANON_KEY`
   - `REACT_APP_TELEGRAM_BOT_USERNAME`
3. Vercel автоматически соберет и задеплоит проект

### Ручной деплой
```bash
npm install -g vercel
vercel --prod
```

### После деплоя
Обновите `WEBAPP_URL` в боте на новый URL от Vercel.

## 🛠 Быстрый старт

### 1. Настройка базы данных
```sql
-- Выполните SQL из cfsick/baza.sql в Supabase
```

### 2. Настройка бота
```python
# В cfsick/main.py
CONFIG = {
    "BOT_TOKEN": "ваш_токен_бота",
    "SUPABASE_URL": "https://ваш-проект.supabase.co",
    "SUPABASE_KEY": "ваш_service_role_key",
    "WEBAPP_URL": "https://ваш-сайт.com"
}
```

### 3. Настройка фронтенда
```bash
cd pin-up-mobile-casino
cp .env.example .env
# Заполните .env файл
npm install
npm run dev
```

### 4. Запуск
```bash
# Терминал 1: Запуск бота
cd cfsick
python main.py

# Терминал 2: Запуск фронтенда
cd pin-up-mobile-casino
npm run dev
```

## 🔧 Технические детали

### Серверная логика игр
```typescript
// Пример из api/supabase.ts
const gameResult = await api.playGame(userId, 'Слоты', bet);
// Результат уже определен сервером на основе win_rate
```

### Realtime обновления
```typescript
// Подписка на изменения пользователя
const channel = supabase
  .channel(`user-${user.id}`)
  .on('postgres_changes', 
    { event: 'UPDATE', table: 'users', filter: `id=eq.${user.id}` },
    (payload) => {
      // Мгновенное обновление баланса и win_rate
    }
  );
```

### Атомарные транзакции
```sql
-- RPC функция для безопасного обновления баланса
CREATE OR REPLACE FUNCTION update_balance_after_game(...)
-- Использует FOR UPDATE для предотвращения race conditions
```

## 📋 Полная инструкция

Подробная инструкция по настройке находится в файле `pin-up-mobile-casino/SETUP.md`

## 🎮 Как это работает

1. **Пользователь** открывает бота и нажимает "🎰 ИГРАТЬ"
2. **Telegram WebApp** автоматически авторизует пользователя
3. **Фронтенд** получает данные пользователя и подписывается на обновления
4. **Игрок** делает ставку в игре
5. **Сервер** получает win_rate из базы и определяет результат
6. **Анимация** подстраивается под серверный результат
7. **Баланс** обновляется атомарно через RPC функцию
8. **Воркер** может изменить win_rate → следующая игра использует новый процент

## 🔒 Безопасность

- ✅ Результат игры определяется **только на сервере**
- ✅ Клиент не может повлиять на исход игры
- ✅ Атомарные транзакции предотвращают дублирование ставок
- ✅ RLS (Row Level Security) можно настроить для дополнительной защиты
- ✅ SERVICE_ROLE_KEY используется только в боте, ANON_KEY в фронтенде

---

**Результат**: Полностью интегрированная система, где бот и сайт работают как единое целое. Воркер может управлять "удачей" игроков в реальном времени, а вся логика защищена на серверном уровне.