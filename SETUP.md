# Настройка интеграции Telegram бота и казино

## 1. Настройка Supabase

### Создание таблиц
Выполните SQL из файла `../cfsick/baza.sql` в SQL Editor вашего Supabase проекта.

### Настройка RLS (Row Level Security)
```sql
-- Отключаем RLS для простоты (в продакшене настройте правильные политики)
ALTER TABLE users DISABLE ROW LEVEL SECURITY;
ALTER TABLE deposits DISABLE ROW LEVEL SECURITY;
ALTER TABLE promocodes DISABLE ROW LEVEL SECURITY;
ALTER TABLE game_logs DISABLE ROW LEVEL SECURITY;
```

### Получение ключей
1. Перейдите в Settings → API
2. Скопируйте `Project URL` и `anon public` ключ

## 2. Настройка Telegram бота

### В файле `../cfsick/main.py`:
```python
CONFIG = {
    "BOT_TOKEN": "8560222423:AAHBW_yOLsGHOhDqFRyQY2JTc2WnpImsbcY", 
    "SUPABASE_URL": "https://ваша-ссылка.supabase.co",
    "SUPABASE_KEY": "ВАШ_SERVICE_ROLE_KEY", 
    "WEBAPP_URL": "https://ссылка-на-ваш-сайт.com"
}
```

### Установка зависимостей:
```bash
cd ../cfsick
pip install aiogram supabase
```

### Запуск бота:
```bash
python main.py
```

## 3. Настройка фронтенда

### Создание .env файла:
```bash
cp .env.example .env
```

### Заполните .env:
```
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key
REACT_APP_TELEGRAM_BOT_USERNAME=PinUp_PlayBot
```

### Установка зависимостей:
```bash
npm install
```

### Запуск в режиме разработки:
```bash
npm run dev
```

### Сборка для продакшена:
```bash
npm run build
```

## 4. Тестирование интеграции

1. Запустите бота
2. Запустите фронтенд
3. Откройте бота в Telegram
4. Нажмите "🎰 ИГРАТЬ" - должен открыться WebApp
5. В панели воркера (`/worker`) измените win_rate пользователя
6. Проверьте, что изменения влияют на результаты игр

## 5. Основные функции

### Серверная логика игр:
- Результат игры определяется на основе `win_rate` из базы данных
- Если `win_rate = 90%`, то в 90% случаев игрок выиграет
- Анимация подстраивается под серверный результат

### Realtime обновления:
- Изменения баланса и win_rate в боте мгновенно отражаются на сайте
- Используется Supabase Realtime

### Панель воркера:
- `/worker` в боте показывает список рефералов
- Возможность изменить баланс и процент удачи
- Управление банами

### Авторизация через Telegram:
- Автоматическое получение данных пользователя из Telegram WebApp
- Синхронизация фото профиля и никнейма
- Создание пользователя при первом входе

## 6. Структура проекта

```
pin-up-mobile-casino/
├── api/
│   └── supabase.ts          # API для работы с базой данных
├── components/
│   ├── GameModal.tsx        # Модальные окна игр с серверной логикой
│   ├── Header.tsx           # Шапка с фото профиля
│   └── ...
├── App.tsx                  # Главный компонент с Realtime
└── ...

../cfsick/
├── main.py                  # Telegram бот с панелью воркера
└── baza.sql                 # SQL схема базы данных
```

## 7. Важные особенности

- **Подкрутка игр**: Результат определяется сервером на основе win_rate
- **Realtime**: Изменения в боте мгновенно отражаются на сайте
- **Фото профиля**: Автоматически загружается из Telegram
- **Логирование**: Все игры записываются в game_logs для аналитики
- **Безопасность**: Используйте SERVICE_ROLE_KEY только в боте, ANON_KEY в фронтенде